# @aidbox-forms/site

Storybook site for Aidbox Forms Renderer. Runs the renderer against sample questionnaires with switchable themes.

## Commands

- `pnpm dev` – start Storybook
- `pnpm build` – build static Storybook to `storybook-static`
